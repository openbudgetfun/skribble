---
skribble: patch
---

Add a font specimen page to the storybook (every glyph of the bundled
Skribble typeface across weights/styles/sizes), deploy the storybook as a
live web preview alongside the docs site on GitHub Pages, and add a Live
Showcase section to the docs.
