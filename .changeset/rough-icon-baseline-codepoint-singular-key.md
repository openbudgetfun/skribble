---
skribble: patch
---

# Accept `codepoint[]` as a minimal unresolved baseline key

The `--unresolved-baseline` loader in `generate_material_rough_icons.dart` now also reads the all-lowercase singular `codepoint` key for minimal baseline objects, completing the accepted lowercase spellings, with parser test coverage.
