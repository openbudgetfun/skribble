---
skribble: patch
---

# Accept `code-point` as a baseline entry codepoint alias for unresolved baseline parsing.

This applies to object entries inside baseline `unresolved[]` and `icons[]` lists.
