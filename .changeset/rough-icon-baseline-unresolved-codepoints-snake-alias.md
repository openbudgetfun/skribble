---
skribble: patch
---

Accept `unresolved_codepoints[]` as a minimal unresolved baseline key for rough icon regression checks.

This adds another snake_case compatibility alias alongside existing baseline key variants.
