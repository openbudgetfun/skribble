---
skribble: patch
---

# Accept singular `unresolved_codepoint[]` minimal baseline keys

The `--unresolved-baseline` loader in `generate_material_rough_icons.dart` now also reads the singular snake_case `unresolved_codepoint` key for minimal baseline objects, completing the accepted snake_case spellings, with parser test coverage.
