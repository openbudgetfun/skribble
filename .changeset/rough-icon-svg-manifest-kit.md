---
skribble: patch
---

# Add `svg-manifest` icon-kit support to icon generation

SVG sets can be processed with `--kit svg-manifest --manifest <path>`. Includes manifest parser tests, docs updates, and an example manifest file.
