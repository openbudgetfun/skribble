---
skribble: minor
---

# Add rough hand-drawn Material icon support

Introduces `WiredIcon` and `WiredSvgIcon` for sketch-style icon rendering,
adds the Material SVG-to-Dart generation pipeline, and wires rough icon
rendering into `WiredIconButton` and `WiredFloatingActionButton`.
