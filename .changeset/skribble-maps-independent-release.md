---
skribble_maps: patch
---

# Release Skribble Maps independently

Give `skribble_maps` its own Monochange release identity and the `skribble_maps/v<version>` tag namespace. Breaking changes in `skribble` now propagate as breaking changes in the maps package.
