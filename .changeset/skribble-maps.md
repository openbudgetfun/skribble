---
skribble_maps: minor
---

# Add hand-drawn vector maps without a proprietary SDK

Introduces a widgets-only map package with direct MVT rendering, an explicit OpenFreeMap example source, PMTiles support, interactive Wired pins and markers, rough routes and areas, zoom controls, attribution, and bounded in-memory caching.
