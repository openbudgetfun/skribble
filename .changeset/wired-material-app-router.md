---
skribble: patch
---

Add `WiredMaterialApp.router` so router-based Flutter apps can keep
`MaterialApp.router` configuration and `WiredTheme` synchronized while using
Skribble's hand-drawn app-level theming.
