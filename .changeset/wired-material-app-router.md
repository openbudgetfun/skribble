---
skribble: patch
---

Add `WiredMaterialApp.router` so Skribble apps can use router-based
`MaterialApp` bootstrapping while keeping `WiredTheme` synchronized with the
active Material theme configuration.
