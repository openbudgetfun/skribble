# Emoji SVG Sources

Place clean SVG source files for hand-drawn emoji in this directory.

Each SVG should contain a single emoji glyph at a consistent viewBox size
(e.g. `0 0 24 24` or `0 0 36 36`). After adding files here, register them in
`../tool/skribble_emoji.manifest.json` and run the rough icon generator.
